{
  "name": "thetisisland",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync-cms-config": "cp public/admin/config.yml public/admin-sveltia/config.yml",
    "dev": "npm run sync-cms-config && astro dev",
    "build": "npm run sync-cms-config && astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.1",
    "@rollup/plugin-yaml": "^4.1.2",
    "@tailwindcss/typography": "^0.5.19",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.1",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
